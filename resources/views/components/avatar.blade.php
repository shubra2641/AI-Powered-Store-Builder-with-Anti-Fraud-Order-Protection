<div class="avatar-circle-wrapper {{ $class }} avatar-{{ $size }} d-flex align-center justify-center overflow-hidden border-radius-circle">
    <img src="{{ $src }}" 
         alt="{{ $name }}" 
         class="avatar-circle w-full h-full object-cover">
</div>
